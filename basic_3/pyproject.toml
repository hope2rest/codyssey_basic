[project]
name = "codyssey-basic-3"
version = "1.0.0"
description = "MAC 연산 기반 패턴 매칭 학습 성취도 자동 채점 (pytest 기반)"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["missions"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
